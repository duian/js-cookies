import should from 'should';
import cookies from 'cookies';

describe('Cookies', () => {
  it('cookie.setItem() should set a value', () => {

  });

  it('cookie.getItem() should return true', () => {

  });

  it('cookie.hasItem() should return a boolean', () => {

  })

  it('cookies.removeItem() should remove item', () => {

  });

  it('cookies.keys() should return a string', () => {

  });
})
